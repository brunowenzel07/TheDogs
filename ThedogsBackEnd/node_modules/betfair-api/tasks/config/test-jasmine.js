module.exports = function(grunt) {

  grunt.config.set('jasmine_node', {
    coverage: {
      savePath: './target/test-reports/coverage/',
      print: 'text-summary',
      excludes: ['test/**/*.js', 'config/**/*.js', 'tasks/**/*.js'],
      branches: 100,
      functions: 1000,
      statements: 100,
      lines: 1000
    },
    projectRoot: '.',
    specFolders: ['./test'],
    options: {
      verbose: true,
      jUnit: {
        report: true,
        savePath: './target/test-reports/',
        useDotNotation: true,
        consolidate: true
      }
    }
  });

  grunt.loadNpmTasks('grunt-jasmine-node-coverage');
};
