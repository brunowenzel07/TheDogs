module.exports = function(grunt) {

  grunt.config.set('jshint', {

    run: {
      options: {
        jshintrc: true
      },
      files: {
        src: [
          '*.js',
          'config/**/*.js',
          'tasks/**/*.js'
        ]
      }
    }
  });

  grunt.loadNpmTasks('grunt-contrib-jshint');
};
