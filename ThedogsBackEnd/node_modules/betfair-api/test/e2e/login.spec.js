var api = require('../../index');

jasmine.getEnv().defaultTimeoutInterval = 30000;

describe('login', function() {

  beforeEach(function() {
  });

  it('should login with the certificate and key', function(done) {

    api.login(function(err, resp) {
      expect(err).toBeNull();
      expect(resp).toBeTruthy();
      done();
    });

  });

});
